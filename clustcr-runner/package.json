{
  "name": "@platforma-open/milaboratories.tcr-clustering-clusttcr.clustcr-runner",
  "version": "0.0.0",
  "description": "clusTCR runner: a docker image (micromamba env + baked run_clustcr.py) for the clustering step.",
  "files": [
    "./dist/**/*"
  ],
  "type": "module",
  "scripts": {
    "build": "block-tools software build",
    "do-pack": "shx rm -f *.tgz && block-tools software build && pnpm pack && shx mv platforma-open*.tgz package.tgz",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@platforma-open/milaboratories.runenv-python-3": "catalog:",
    "@platforma-sdk/block-tools": "catalog:",
    "shx": "catalog:"
  },
  "block-software": {
    "entrypoints": {
      "runner": {
        "docker": {
          "artifact": {
            "type": "docker",
            "context": "./context",
            "dockerfile": "Dockerfile"
          },
          "cmd": [
            "micromamba",
            "run",
            "-p",
            "/env",
            "python",
            "/app/run_clustcr.py"
          ]
        }
      }
    }
  }
}
