{
  "name": "@platforma-open/milaboratories.tcr-clustering-clusttcr.workflow",
  "version": "0.0.0",
  "description": "Tengo-based template",
  "type": "module",
  "scripts": {
    "build": "shx rm -rf dist && pl-tengo build",
    "format": "/usr/bin/env emacs --script ./format.el || echo 'No emacs.'",
    "do-pack": "shx rm -f *.tgz && pnpm pack && shx mv *.tgz package.tgz",
    "check": "pl-tengo check"
  },
  "dependencies": {
    "@platforma-open/milaboratories.tcr-clustering-clusttcr.clustcr-runner": "workspace:*",
    "@platforma-open/milaboratories.tcr-clustering-clusttcr.software": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "shx": "catalog:"
  }
}